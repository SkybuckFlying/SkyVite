{$MODE DELPHIUNICODE}
unit Vendor.Golang.Org.X.Sys.Unix.ZtypesSolarisAmd64;

interface

// cgo -godefs types_solaris.go | go run mkpost.go
// Code generated by the command above; see README.md. DO NOT EDIT.

{$IFDEF FPC}
uses
  SysUtils;
{$ELSE}
uses
  System.SysUtils;
{$ENDIF}

const
    ConstSizeofPtr = $8;
    ConstSizeofShort = $2;
    ConstSizeofInt = $4;
    ConstSizeofLong = $8;
    ConstSizeofLongLong = $8;
    Const_PathMax = $400;
    Const_MaxHostNameLen = $100;

    Const_AT_FDCWD = $ffd19553;
    Const_AT_SYMLINK_NOFOLLOW = $1000;
    Const_AT_SYMLINK_FOLLOW = $2000;
    Const_AT_REMOVEDIR = $1;
    Const_AT_EACCESS = $4;

    ConstSizeofIfMsghdr = $54;
    ConstSizeofIfData = $44;
    ConstSizeofIfaMsghdr = $14;
    ConstSizeofRtMsghdr = $4c;
    ConstSizeofRtMetrics = $28;

    ConstSizeofSockaddrInet4 = $10;
    ConstSizeofSockaddrInet6 = $20;
    ConstSizeofSockaddrAny = $fc;
    ConstSizeofSockaddrUnix = $6e;
    ConstSizeofSockaddrDatalink = $fc;
    ConstSizeofLinger = $8;
    ConstSizeofIovec = $10;
    ConstSizeofIPMreq = $8;
    ConstSizeofIPv6Mreq = $14;
    ConstSizeofMsghdr = $30;
    ConstSizeofCmsghdr = $c;
    ConstSizeofInet4Pktinfo = $c;
    ConstSizeofInet6Pktinfo = $14;
    ConstSizeofIPv6MTUInfo = $24;
    ConstSizeofICMPv6Filter = $20;

    ConstSizeofBpfVersion = $4;
    ConstSizeofBpfStat = $80;
    ConstSizeofBpfProgram = $10;
    ConstSizeofBpfInsn = $8;
    ConstSizeofBpfHdr = $14;

    Const_POLLERR = $8;
    Const_POLLHUP = $10;
    Const_POLLIN = $1;
    Const_POLLNVAL = $20;
    Const_POLLOUT = $4;
    Const_POLLPRI = $2;
    Const_POLLRDBAND = $80;
    Const_POLLRDNORM = $40;
    Const_POLLWRBAND = $100;
    Const_POLLWRNORM = $4;

type
    // Basic C-type aliases
    C_short = SmallInt;
    C_int = Integer;
    C_long = Int64;
    C_long_long = Int64;

    // Forward declarations for pointers
    PTimespec = ^TTimespec;
    PTimeval = ^TTimeval;
    PTimeval32 = ^TTimeval32;
    PTms = ^TTms;
    PUtimbuf = ^TUtimbuf;
    PRusage = ^TRusage;
    PRlimit = ^TRlimit;
    PStat_t = ^TStat_t;
    PFlock_t = ^TFlock_t;
    PDirent = ^TDirent;
    PStatvfs_t = ^TStatvfs_t;
    PRawSockaddrInet4 = ^TRawSockaddrInet4;
    PRawSockaddrInet6 = ^TRawSockaddrInet6;
    PRawSockaddrUnix = ^TRawSockaddrUnix;
    PRawSockaddrDatalink = ^TRawSockaddrDatalink;
    PRawSockaddr = ^TRawSockaddr;
    PRawSockaddrAny = ^TRawSockaddrAny;
    PLinger = ^TLinger;
    PIovec = ^TIovec;
    PIPMreq = ^TIPMreq;
    PIPv6Mreq = ^TIPv6Mreq;
    PMsghdr = ^TMsghdr;
    PCmsghdr = ^TCmsghdr;
    PInet4Pktinfo = ^TInet4Pktinfo;
    PInet6Pktinfo = ^TInet6Pktinfo;
    PIPv6MTUInfo = ^TIPv6MTUInfo;
    PICMPv6Filter = ^TICMPv6Filter;
    PFdSet = ^TFdSet;
    PUtsname = ^TUtsname;
    PUstat_t = ^TUstat_t;
    PIfMsghdr = ^TIfMsghdr;
    PIfData = ^TIfData;
    PIfaMsghdr = ^TIfaMsghdr;
    PRtMsghdr = ^TRtMsghdr;
    PRtMetrics = ^TRtMetrics;
    PBpfVersion = ^TBpfVersion;
    PBpfStat = ^TBpfStat;
    PBpfProgram = ^TBpfProgram;
    PBpfInsn = ^TBpfInsn;
    PBpfTimeval = ^TBpfTimeval;
    PBpfHdr = ^TBpfHdr;
    PTermios = ^TTermios;
    PTermio = ^TTermio;
    PWinsize = ^TWinsize;
    PPollFd = ^TPollFd;

    // Struct definitions (mapped to Delphi Records)
    TTimespec = record
        mSec : Int64;
        mNsec : Int64;
    end;

    TTimeval = record
        mSec : Int64;
        mUsec : Int64;
    end;

    TTimeval32 = record
        mSec : Integer;
        mUsec : Integer;
    end;

    TTms = record
        mUtime : Int64;
        mStime : Int64;
        mCutime : Int64;
        mCstime : Int64;
    end;

    TUtimbuf = record
        mActime : Int64;
        mModtime : Int64;
    end;

    TRusage = record
        mUtime : TTimeval;
        mStime : TTimeval;
        mMaxrss : Int64;
        mIxrss : Int64;
        mIdrss : Int64;
        mIsrss : Int64;
        mMinflt : Int64;
        mMajflt : Int64;
        mNswap : Int64;
        mInblock : Int64;
        mOublock : Int64;
        mMsgsnd : Int64;
        mMsgrcv : Int64;
        mNsignals : Int64;
        mNvcsw : Int64;
        mNivcsw : Int64;
    end;

    TRlimit = record
        mCur : UInt64;
        mMax : UInt64;
    end;

    TGid_t = Cardinal;

    TStat_t = record
        mDev : UInt64;
        mIno : UInt64;
        mMode : Cardinal;
        mNlink : Cardinal;
        mUid : Cardinal;
        mGid : Cardinal;
        mRdev : UInt64;
        mSize : Int64;
        mAtim : TTimespec;
        mMtim : TTimespec;
        mCtim : TTimespec;
        mBlksize : Integer;
        mBlocks : Int64;
        mFstype : array[0..15] of AnsiChar; // [16]int8
    end;

    TFlock_t = record
        mType : SmallInt;
        mWhence : SmallInt;
        mStart : Int64;
        mLen : Int64;
        mSysid : Integer;
        mPid : Integer;
        mPad : array[0..3] of Int64; // [4]int64
    end;

    TDirent = record
        mIno : UInt64;
        mOff : Int64;
        mReclen : Word;
        mName : array[0..0] of AnsiChar; // [1]int8
        m_ : array[0..4] of Byte; // [5]byte
    end;

    TFsblkcnt_t = UInt64;

    TStatvfs_t = record
        mBsize : UInt64;
        mFrsize : UInt64;
        mBlocks : UInt64;
        mBfree : UInt64;
        mBavail : UInt64;
        mFiles : UInt64;
        mFfree : UInt64;
        mFavail : UInt64;
        mFsid : UInt64;
        mBasetype : array[0..15] of AnsiChar; // [16]int8
        mFlag : UInt64;
        mNamemax : UInt64;
        mFstr : array[0..31] of AnsiChar; // [32]int8
    end;

    TRawSockaddrInet4 = record
        mFamily : Word;
        mPort : Word;
        mFlowinfo : Cardinal;
        mAddr : array[0..3] of Byte; // [4]byte /* in_addr */
        mZero : array[0..7] of AnsiChar; // [8]int8
    end;

    TRawSockaddrInet6 = record
        mFamily : Word;
        mPort : Word;
        mFlowinfo : Cardinal;
        mAddr : array[0..15] of Byte; // [16]byte /* in6_addr */
        mScope_id : Cardinal;
        m_ : Cardinal; // uint32
    end;

    TRawSockaddrUnix = record
        mFamily : Word;
        mPath : array[0..107] of AnsiChar; // [108]int8
    end;

    TRawSockaddrDatalink = record
        mFamily : Word;
        mIndex : Word;
        mType : Byte;
        mNlen : Byte;
        mAlen : Byte;
        mSlen : Byte;
        mData : array[0..243] of AnsiChar; // [244]int8
    end;

    TRawSockaddr = record
        mFamily : Word;
        mData : array[0..13] of AnsiChar; // [14]int8
    end;

    TRawSockaddrAny = record
        mAddr : TRawSockaddr;
        mPad : array[0..235] of AnsiChar; // [236]int8
    end;

    TSocklen = Cardinal;

    TLinger = record
        mOnoff : Integer;
        mLinger : Integer;
    end;

    TIovec = record
        mBase : PAnsiChar; // *int8 -> PAnsiChar
        mLen : UInt64;
    end;

    TIPMreq = record
        mMultiaddr : array[0..3] of Byte; // [4]byte /* in_addr */
        mInterface : array[0..3] of Byte; // [4]byte /* in_addr */
    end;

    TIPv6Mreq = record
        mMultiaddr : array[0..15] of Byte; // [16]byte /* in6_addr */
        mInterface : Cardinal;
    end;

    TMsghdr = record
        mName : PByte; // *byte
        mNamelen : Cardinal;
        mIov : PIovec;
        mIovlen : Integer;
        mAccrights : PAnsiChar; // *int8 -> PAnsiChar
        mAccrightslen : Integer;
        m_ : array[0..3] of Byte; // [4]byte
    end;

    TCmsghdr = record
        mLen : Cardinal;
        mLevel : Integer;
        mType : Integer;
    end;

    TInet4Pktinfo = record
        mIfindex : Cardinal;
        mSpec_dst : array[0..3] of Byte; // [4]byte /* in_addr */
        mAddr : array[0..3] of Byte; // [4]byte /* in_addr */
    end;

    TInet6Pktinfo = record
        mAddr : array[0..15] of Byte; // [16]byte /* in6_addr */
        mIfindex : Cardinal;
    end;

    TIPv6MTUInfo = record
        mAddr : TRawSockaddrInet6;
        mMtu : Cardinal;
    end;

    TICMPv6Filter = record
        mFilt : array[0..7] of Cardinal; // [8]uint32
    end;

    TFdSet = record
        mBits : array[0..1023] of Int64; // [1024]int64
    end;

    TUtsname = record
        mSysname : array[0..256] of Byte; // [257]byte
        mNodename : array[0..256] of Byte; // [257]byte
        mRelease : array[0..256] of Byte; // [257]byte
        mVersion : array[0..256] of Byte; // [257]byte
        mMachine : array[0..256] of Byte; // [257]byte
    end;

    TUstat_t = record
        mTfree : Int64;
        mTinode : UInt64;
        mFname : array[0..5] of AnsiChar; // [6]int8
        mFpack : array[0..5] of AnsiChar; // [6]int8
        m_ : array[0..3] of Byte; // [4]byte
    end;

    TIfMsghdr = record
        mMsglen : Word;
        mVersion : Byte;
        mType : Byte;
        mAddrs : Integer;
        mFlags : Integer;
        mIndex : Word;
        mData : TIfData;
    end;

    TIfData = record
        mType : Byte;
        mAddrlen : Byte;
        mHdrlen : Byte;
        mMtu : Cardinal;
        mMetric : Cardinal;
        mBaudrate : Cardinal;
        mIpackets : Cardinal;
        mIerrors : Cardinal;
        mOpackets : Cardinal;
        mOerrors : Cardinal;
        mCollisions : Cardinal;
        mIbytes : Cardinal;
        mObytes : Cardinal;
        mImcasts : Cardinal;
        mOmcasts : Cardinal;
        mIqdrops : Cardinal;
        mNoproto : Cardinal;
        mLastchange : TTimeval32;
    end;

    TIfaMsghdr = record
        mMsglen : Word;
        mVersion : Byte;
        mType : Byte;
        mAddrs : Integer;
        mFlags : Integer;
        mIndex : Word;
        mMetric : Integer;
    end;

    TRtMsghdr = record
        mMsglen : Word;
        mVersion : Byte;
        mType : Byte;
        mIndex : Word;
        mFlags : Integer;
        mAddrs : Integer;
        mPid : Integer;
        mSeq : Integer;
        mErrno : Integer;
        mUse : Integer;
        mInits : Cardinal;
        mRmx : TRtMetrics;
    end;

    TRtMetrics = record
        mLocks : Cardinal;
        mMtu : Cardinal;
        mHopcount : Cardinal;
        mExpire : Cardinal;
        mRecvpipe : Cardinal;
        mSendpipe : Cardinal;
        mSsthresh : Cardinal;
        mRtt : Cardinal;
        mRttvar : Cardinal;
        mPksent : Cardinal;
    end;

    TBpfVersion = record
        mMajor : Word;
        mMinor : Word;
    end;

    TBpfStat = record
        mRecv : UInt64;
        mDrop : UInt64;
        mCapt : UInt64;
        m_ : array[0..12] of UInt64; // [13]uint64
    end;

    TBpfProgram = record
        mLen : Cardinal;
        mInsns : PBpfInsn;
    end;

    TBpfInsn = record
        mCode : Word;
        mJt : Byte;
        mJf : Byte;
        mK : Cardinal;
    end;

    TBpfTimeval = record
        mSec : Integer;
        mUsec : Integer;
    end;

    TBpfHdr = record
        mTstamp : TBpfTimeval;
        mCaplen : Cardinal;
        mDatalen : Cardinal;
        mHdrlen : Word;
        m_ : array[0..1] of Byte; // [2]byte
    end;

    TTermios = record
        mIflag : Cardinal;
        mOflag : Cardinal;
        mCflag : Cardinal;
        mLflag : Cardinal;
        mCc : array[0..18] of Byte; // [19]uint8
        m_ : array[0..0] of Byte; // [1]byte
    end;

    TTermio = record
        mIflag : Word;
        mOflag : Word;
        mCflag : Word;
        mLflag : Word;
        mLine : ShortInt;
        mCc : array[0..7] of Byte; // [8]uint8
        m_ : array[0..0] of Byte; // [1]byte
    end;

    TWinsize = record
        mRow : Word;
        mCol : Word;
        mXpixel : Word;
        mYpixel : Word;
    end;

    TPollFd = record
        mFd : Integer;
        mEvents : SmallInt;
        mRevents : SmallInt;
    end;

implementation

end.