unit Vendor.Google.Golang.Org.Protobuf.Proto.Types;

{$MODE DELPHIUNICODE}

interface

// Common types for the proto package to avoid circular dependencies

type
  TMarshalOptions = record
    // Fields would be here (e.g. Pragma, UseCachedSize, etc.)
    // For now, it's a placeholder to allow methods to be attached via helpers.
  end;

implementation

end.
